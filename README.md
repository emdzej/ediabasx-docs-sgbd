# ediabasx-docs-sgbd

Documentation for EdiabasX SGBD.

## Setup

Install Python dependencies:

```bash
pip install -r requirements.txt
```

Run the docs locally:

```bash
pnpm dev
```

Build the site:

```bash
pnpm build
```
